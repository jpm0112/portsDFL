# This file is intentionally (almost) empty. Its mere presence marks the `src` folder as a
# Python "package", which is what lets the other modules import each other with relative
# imports like `from .data_prep import prepare` and lets you run `python -m src.compare_models`.
