# Bayesian hierarchical service-time models (PyMC).
# Independent of the prediction_models / optimizers stack.
pymc>=5.10
arviz>=0.17
numpy>=1.26
pandas>=2.2
scikit-learn>=1.5
pyyaml>=6.0
matplotlib>=3.8   # posterior / diagnostic figures
pytest>=8.0       # tests/
