# Prediction-only dependencies for the portable tree predictor (rf, xgb, lgbm).
# No PyTorch: tree models run on CPU, and a GPU gives no benefit for inference.
# Versions pinned to what the models were trained with, so the saved pickles load
# without scikit-learn / xgboost version-mismatch warnings.
pandas>=2.0
numpy>=1.26
joblib>=1.3
scikit-learn==1.9.0
xgboost==3.2.0
lightgbm==4.6.0
category-encoders==2.9.0
