# CUDA-tag-agnostic. setup.ps1 picks the correct PyTorch wheel index URL
# based on the host CUDA version detected via nvidia-smi.
#
# Manual install:
#   pip install --extra-index-url https://download.pytorch.org/whl/cu121 -r requirements.txt
#
# Replace cu121 with cu118 / cu124 / cpu as appropriate.

# Optimizer stack (MILP + DFL hooks): prediction_models imports the bap_optim
# package, so it inherits the optimizers' requirements. This also brings in
# numpy / pandas / pyomo / pyepo / gurobipy — no need to repeat them here.
-r ../optimizers/requirements.txt

# Core ML / scientific
scikit-learn>=1.5

# Deep learning
torch>=2.4
torchvision

# Reference predictors
lightgbm>=4.3
xgboost>=2.0              # XGBoost benchmark (models/xgb.py)
pytabkit>=1.4              # RealMLP (models/realmlp.py)
pytorch_tabular>=1.1      # NODE (models/node.py)
tabm>=0.0.3               # TabM (models/tabm.py)

# Hyperparameter tuning
optuna>=3.6

# Categorical encoding
category-encoders>=2.6

# Plotting (build_report.py PDF + plan_week.py Gantt)
matplotlib>=3.8

# Tooling
pytest>=8.0
ruff>=0.5
black>=24.0
