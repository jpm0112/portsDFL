# Prediction-only dependencies for the portable tree predictor (rf, xgb, lgbm).
# No PyTorch: tree models run on CPU, and a GPU gives no benefit for inference.
# The estimator + preprocessor libs are pinned to the trained versions so the saved
# pickles load without version-mismatch warnings. numpy/pandas are left as floors
# (verified to load on pandas 3.x); pin them too if a future major breaks the unpickle.
pandas>=2.0
numpy>=1.26
joblib>=1.3
scikit-learn==1.9.0
xgboost==3.2.0
lightgbm==4.6.0
category-encoders==2.9.0
openpyxl>=3.1        # read weekly .xlsx schedules + the vessel-history workbook (predict_weeks.py)
