# The bundle is assembled/shipped locally; keep heavy + generated files out of git.
.venv/
artifacts/
*_predictions.csv
predictions.csv
# per-week output folder (predict_weeks.py)
predictions/
# operational weekly-schedule inputs (real vessel data) stay local
weekly_schedules/
