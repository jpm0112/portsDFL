# This empty file makes the "tests" folder an importable Python package.
# Python treats any directory containing an __init__.py as a "package", which
# lets test modules be addressed by a dotted path (e.g. prediction_models.tests
# .test_splits). For pytest this mainly matters for avoiding name clashes when
# two test files share the same filename in different folders. There is no code
# to run here -- the file is intentionally (almost) empty.
